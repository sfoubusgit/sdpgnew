Below is the fully rebuilt, hyper-explicit, final Cursor Program-Creation Prompt —
now enhanced so Cursor MUST read and use the ABOUT_THE_PROGRAM.txt description file as an authoritative reference.

This is the single unified prompt you will paste into Cursor to generate ONLY the Guided Prompt Interview Creator.
It incorporates:

✔ All architecture
✔ All UI rules
✔ All JSON structures
✔ All component requirements
✔ All engine logic
✔ All file creation steps
✔ Mandatory synchronization with ABOUT_THE_PROGRAM.txt

This prompt forces Cursor to:

Read the included .txt file

Obey the specification exactly

Implement only what is defined

Match the UI shown in your preview

Follow the entire system behavior described

Avoid randomizer, builder, JSON editor, or other features

This is the final master prompt.

⭐⭐⭐ THE COMPLETE CURSOR SUPER-PROMPT (WITH DESCRIPTION FILE INTEGRATION) ⭐⭐⭐

Paste this into Cursor exactly as written:

🟣 CURSOR SUPER-PROMPT — Build ONLY the Guided Prompt Interview Creator

IMPORTANT: A file named ABOUT_THE_PROGRAM.txt exists in the project.
This file contains the complete definition and description of the program.
You MUST read, follow, and use that file as the authoritative specification.

If any instruction in this prompt appears ambiguous,
the description file overrides everything.
Use ABOUT_THE_PROGRAM.txt as a reference for architecture, behavior, UI, and system logic.

============================================================
1. PROJECT GOAL
============================================================

You must build a standalone Guided Prompt Interview Creator, as described in
ABOUT_THE_PROGRAM.txt.

Only implement this mode.
Do NOT create any randomizer, builder mode, tag library, JSON editor, or extra features.

The program consists of:

A decision-tree driven interview

Refinement questions

Weight sliders

A right-side live prompt preview

A final assembled Stable Diffusion prompt

The structure, logic, UI style, and behavior MUST match both:

This prompt, and

The ABOUT_THE_PROGRAM.txt description file

============================================================
2. VISUAL UI REQUIREMENTS (MUST MATCH PREVIEW + DESCRIPTION FILE)
============================================================

The UI MUST visually match the mockup aesthetic described in ABOUT_THE_PROGRAM.txt:

Palette:

Background: #0F0F12

Card: #1A1A1F

Accent glow: #6E56F9

Text primary: white

Text secondary: #B0B0C3

Borders: rgba(255,255,255,0.04)

Layout:

Two-column layout

Left: question card

Right: live preview

65% / 35% ratio

32px gap

Centered main card

Components:

Large rounded cards (18–20px radius)

Soft shadows

Accent glow ring

Smooth animated transitions

Subtle hover glows

Pill-shaped buttons

Violet-accent weight sliders

Typography:

Inter or Manrope

Title: 28–32px

Question text: 20–22px

Answers: 16–18px

Secondary text: 14–15px

Animation:

Fade/slide between questions

Smooth slider updates

Highlight on active selection

Follow all UI guidelines in the description file.

============================================================
3. PROJECT STRUCTURE
============================================================

Create EXACTLY this file/folder structure:

src/
  components/
    QuestionCard/
      QuestionCard.tsx
      QuestionCard.css
    AnswerButton/
      AnswerButton.tsx
      AnswerButton.css
    RefinementPanel/
      RefinementPanel.tsx
      RefinementPanel.css
    WeightSlider/
      WeightSlider.tsx
      WeightSlider.css
    PreviewPanel/
      PreviewPanel.tsx
      PreviewPanel.css
    NavigationButtons/
      NavigationButtons.tsx
      NavigationButtons.css
    StepIndicator/
      StepIndicator.tsx
      StepIndicator.css

  data/
    interview/
      root.json
      character.json
      environment.json

  hooks/
    useInterviewEngine.ts

  services/
    promptAssembler.ts

  types/
    interview.ts

  pages/
    InterviewPage.tsx

  App.tsx
  main.tsx

============================================================
4. TYPE DEFINITIONS (MANDATORY)
============================================================

Inside src/types/interview.ts, write exactly:

export interface AnswerOption {
  id: string;
  label: string;
  next?: string;
  autoRefine?: boolean;
}

export interface WeightAttribute {
  id: string;
  label: string;
  template: string;
  min: number;
  max: number;
  step: number;
  default: number;
}

export interface RefinementNode {
  id: string;
  question: string;
  answers: AnswerOption[];
  weights?: WeightAttribute[];
}

export interface InterviewNode {
  id: string;
  question: string;
  description?: string;
  answers: AnswerOption[];
  refinements?: RefinementNode[];
  weights?: WeightAttribute[];
}

============================================================
5. JSON DECISION TREE SYSTEM
============================================================

You MUST implement automatic data loading using:

const interviewFiles = import.meta.glob('/src/data/interview/*.json', { eager: true });


Every JSON file contains an array of nodes or a single node.
Cursor MUST merge them into a global node map keyed by "id".

These three JSON files MUST exist:
root.json
{
  "id": "root",
  "question": "What are you imagining?",
  "answers": [
    { "id": "character", "label": "A character", "next": "character-root" },
    { "id": "environment", "label": "An environment", "next": "environment-root" }
  ]
}

character.json

(Multiple nodes — includes a refinement and a weight slider)

[
  {
    "id": "character-root",
    "question": "What gender is the character?",
    "answers": [
      { "id": "female", "label": "Female", "next": "character-age" },
      { "id": "male", "label": "Male", "next": "character-age" }
    ]
  },
  {
    "id": "character-age",
    "question": "What age?",
    "answers": [
      { "id": "young_adult", "label": "Young adult", "next": "character-hair" },
      { "id": "adult", "label": "Adult", "next": "character-hair" }
    ]
  },
  {
    "id": "character-hair",
    "question": "What is the hair like?",
    "answers": [
      { "id": "curly", "label": "Curly", "next": "refine-curly" },
      { "id": "straight", "label": "Straight", "next": null }
    ]
  },
  {
    "id": "refine-curly",
    "question": "How curly?",
    "answers": [
      { "id": "soft_waves", "label": "Soft waves" },
      { "id": "defined_curls", "label": "Defined curls" },
      { "id": "tight_coils", "label": "Tight coils" }
    ],
    "weights": [
      {
        "id": "curl_intensity",
        "label": "Curl Intensity",
        "template": "curl intensity",
        "min": 1.0,
        "max": 1.3,
        "step": 0.01,
        "default": 1.12
      }
    ]
  }
]

environment.json
[
  {
    "id": "environment-root",
    "question": "What type of environment?",
    "answers": [
      { "id": "forest", "label": "Forest", "next": "forest-type" },
      { "id": "city", "label": "City", "next": null }
    ]
  },
  {
    "id": "forest-type",
    "question": "What kind of forest?",
    "answers": [
      { "id": "enchanted", "label": "Enchanted", "next": null },
      { "id": "dark", "label": "Dark & mysterious", "next": null }
    ],
    "weights": [
      {
        "id": "magic_intensity",
        "label": "Magic Intensity",
        "template": "magical atmosphere",
        "min": 1.0,
        "max": 1.4,
        "step": 0.01,
        "default": 1.2
      }
    ]
  }
]

============================================================
6. INTERVIEW ENGINE
============================================================

Inside useInterviewEngine.ts, implement:

State management:

currentNode

history stack

selected answers

selected refinements

weight values

Required methods:
next(answerId)
previous()
setWeight(attrId, value)
reset()
getPreviewPrompt()
isFinished()


These behaviors are described in ABOUT_THE_PROGRAM.txt.
Follow that description exactly.

============================================================
7. PROMPT ASSEMBLER
============================================================

Create src/services/promptAssembler.ts.

This file must:

Combine all answers and refinements

Insert weighted attributes as (template:value)

Produce a natural-language final prompt

Append default negative prompts:

deformed, distorted, extra limbs, low detail, low quality, bad anatomy


The output format MUST match the style described in ABOUT_THE_PROGRAM.txt.

============================================================
8. UI COMPONENT IMPLEMENTATION
============================================================

You MUST create the following components exactly:

QuestionCard
AnswerButton
RefinementPanel
WeightSlider
PreviewPanel
StepIndicator
NavigationButtons


Each component MUST visually follow the preview and the style rules documented in the description file.

Cursor MUST implement CSS for:

rounded cards

shadows

glow rings

slider design

layout spacing

transitions

The UI MUST look like the provided mockup.

============================================================
9. ABSOLUTE RESTRICTIONS
============================================================

You must NOT build:

❌ Prompt randomizer
❌ Classic prompt builder
❌ JSON editing UI
❌ Node editor
❌ Theme system
❌ Preset system
❌ Any mode other than Interview Mode

This program is ONLY the Guided Prompt Interview Creator.

============================================================
10. FINAL INSTRUCTION
============================================================

Build the entire application exactly according to:

✔ this prompt
✔ the UI requirements
✔ the JSON data structure
✔ the component list
✔ the interview engine logic
✔ the prompt assembly rules
✔ the full specification inside ABOUT_THE_PROGRAM.txt

The .txt file MUST be treated as an authoritative reference.
Follow it strictly.

⭐ END OF SUPER-PROMPT ⭐